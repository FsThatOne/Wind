# Smoke Check Report

**Date**: 2026-06-15  
**Sprint**: Sprint 5 — Combat UI Interaction & Navigation  
**Engine**: Godot 4.6.3 / C# / .NET 8  
**QA Plan**: `production/qa/qa-plan-sprint-5-2026-06-14.md`  
**Mode**: `sprint`

---

## Automated Tests

**Status**: PASS

- `Foundation.Tests`: 1344 / 1344 passed.
- Combat UI targeted tests: 49 / 49 passed.
- Vertical slice build: 0 warnings / 0 errors.
- `git diff --check`: passed for recently changed Combat UI files.

### Godot Headless Runner

**Status**: NOT RUN / WARNING

- Command attempted: `godot --headless --log-file /tmp/fengzhi-smoke/godot-smoke-2026-06-15.log --script tests/gdunit4_runner.gd`
- Result: `godot` is not on the current shell `PATH`; command exited with code `127`.
- Supplemental evidence: Godot MCP launched the vertical slice successfully with Godot `4.6.3`; debug output reported no project errors.
- Note: project stop output included a macOS IMK input-method mach port warning. No Godot project error was observed.

---

## Test Coverage

| Story | Type | Test File / Evidence | Coverage Status |
|-------|------|----------------------|-----------------|
| `cu-004` 招式选择面板与预览卡 | UI | `tests/integration/combat-ui/combat_ui_move_selection_panel_test.cs` | COVERED / Manual evidence missing |
| `cu-005` 反制与决胜行动提示 | Integration | `tests/integration/combat-ui/combat_ui_counter_decisive_prompt_test.cs` | COVERED |
| `cu-008` 双焦点与手柄导航 | UI | `tests/integration/combat-ui/combat_ui_dual_focus_navigation_test.cs`; `production/qa/evidence/cu-008-dual-focus-and-gamepad-navigation-evidence.md` | COVERED / PARTIAL PASS |
| `cu-006` 一击决胜演出编排 | Visual/Feel | Story still `backlog` | OUT OF CURRENT MUST-HAVE SCOPE |
| `cu-007` 协同与回合警戒反馈 | Visual/Feel | Story still `backlog` | OUT OF CURRENT MUST-HAVE SCOPE |

**Summary**: 3 covered, 2 manual/partial evidence, 2 backlog / not implemented, 0 missing blocking automated tests for completed must-have stories.

---

## Manual Smoke Checks

### Core Stability

- [x] Game launches without crash — PASS.
- [x] New session / combat prototype starts — PASS.
- [x] Main navigation/input responsiveness — PASS.
- [x] Crash or hang during basic navigation — PASS.

### Sprint Changes And Regression

- [x] `cu-008` keyboard fallback navigation — PASS.
- [x] `cu-008` initial focus and wraparound after fix — PASS.
- [x] `cu-008` code review blockers after fix — PASS WITH SUGGESTIONS.
- [ ] Combat UI base actions — FAIL: user observed “战斗除了招数，缺少调息选项”.

### Data Integrity And Performance

- [-] Save/load — not confirmed in this smoke run.
- [x] Performance — no explicit failure reported.
- [x] Godot MCP run produced normal gameplay log and reached player victory path.

---

## Failing Smoke Item

### Combat UI 缺少调息选项

**Status**: FAIL

**Expected**: From the Sprint 5 QA plan and `cu-004`, the move selection panel should include equipped moves plus basic actions including `Rest/Meditate` / `调息`, `Basic Attack`, and `Use Item`.

**Actual**: Manual smoke observation: “战斗除了招数，缺少调息选项”.

**Impact**: Blocks QA hand-off because it violates a completed must-have story's player-decision loop requirement.

**Suggested Fix Target**: The actual vertical slice UI path, likely `prototypes/fengzhi-vertical-slice/scripts/ui/BossBattleUI.cs`. Foundation contract tests may expose basic fallback actions, but the prototype scene currently appears to render only boss battle moves.

---

## Missing / Partial Evidence

- `cu-004` manual evidence document was not found under `production/qa/evidence/`.
- `cu-008` remains `PARTIAL PASS` because no physical controller is available; real `D-pad / A` and gamepad hover/focus coexistence remain tracked as technical debt.
- `cu-006` and `cu-007` are still `backlog`, so they are not treated as failed implementation in this must-have smoke gate.

---

## Verdict: FAIL

This build should not be handed off to QA yet.

Blocking issue:

- Combat UI base action coverage failed: battle UI currently lacks a visible `调息` option outside the move list.

Required next step:

- Fix the vertical slice battle UI so the player-decision loop exposes `调息` / basic fallback actions as required by `cu-004`.
- Re-run `/smoke-check sprint` after the fix.
