# S7-VS-Outcome-Feedback Smoke Evidence (Code-Level)

> **Date**: 2026-06-23
> **Story**: S7-VS-Outcome-Feedback (Sprint 7 must-have)
> **Phase**: AC7 — 代码层 smoke 通过，owner 实机走查 pending
> **Scope**: dev-story spec [2026-06-23-s7-vs-outcome-feedback.md](../../../../docs/superpowers/specs/2026-06-23-s7-vs-outcome-feedback.md) §7

## 1 · 自动化 Smoke 矩阵 (PASS)

| Layer | 命令 | 结果 |
|---|---|---|
| Foundation build | `dotnet build src/FengZhi.Foundation/FengZhi.Foundation.csproj` | 0 err / 2 warn（无关：BossPhaseSystem 旧 warn） |
| feng-zhi build | `dotnet build feng-zhi/FengZhi.csproj` | 0 err / 0 new warn |
| Foundation tests | `dotnet test tests/Foundation/Foundation.Tests.csproj` | **1403/1403 PASS**（1399 baseline + 4 outcome-feedback 新增） |
| Outcome-Feedback 新增 tests | `dotnet test --filter "FullyQualifiedName~JiangnanFlowMindset"` | **4/4 PASS**（Spare/Defeat/ZoneCross/SnapshotContract） |
| Godot import | `Godot --headless --import --quit-after 60` (mono 4.7.x) | 0 err / 0 import warn |

## 2 · 代码层确认的链路

```
Owner clicks SpareButton/DefeatButton
  → JiangnanOutcomeGame.HandleChoice(MindsetChoice)
    → flow.ApplyOutcomeChoice(choice)
      → MindsetOutcomeShifts.ResolveShifts(choice)     [Foundation]
      → MindsetService.ApplyShifts(shifts)             [Foundation]
        → MindsetState 更新（Morality / Resolve 钳位到 [-50, +50]）
        → EventBus.Publish<MindsetShiftedEvent>(...)   x N
        → EventBus.Publish<MindsetZoneChangedEvent>(...)  if 跨界
        → EventBus.Publish<MoralityTierChangedEvent>(...) if 跨档
      → flow.LastMindsetShiftSnapshot = new (old/newState, old/newZone, old/newTier, shifts)
    → snapshot 渲染：
      ZoneNameLabel       = MindsetZoneLiteraryNames.GetZoneName(NewZone)
      ResolveBefore/After = MindsetPresentationService.GetResolveDescription(Old/NewState)
      WorldlyBefore/After = MindsetPresentationService.GetWorldlyDescription(Old/NewState)
      MoralityTierLabel   = MindsetZoneLiteraryNames.GetMoralityTierName(Old/New tier)
      _blurredPanel.SelfModulate = WarmthToModulate(GetVisualParams(NewZone).Warmth)
    → BlurredPanel 可见，ChoicePanel 隐藏
Owner clicks ContinueButton
  → flow.GoToExplore() → ChangeSceneToFile(jiangnan_riverside.tscn)
```

## 3 · Owner 实机走查 Pending Checklist

DoD（spec §7）要求 owner 在 Godot 4.7-mono 编辑器内完成：

- [ ] 从 StartCave 进入 → 江南 explore → 战斗触发 → 战斗胜利
- [ ] 进入 outcome scene 看到 ChoicePanel 标题文案根据胜负切换
- [ ] 点击「放过」：BlurredPanel 显示，Zone 名（默认 `中庸 · 心如平湖` → `中庸` 或可能跨界）+ Resolve/Worldly 前后描述对比 + 道义 `常人 → 仁善有声`（如跨 +15）；panel 应为半透明深底 + 轻微暖色 modulate
- [ ] 退回 explore，重新触发战斗，胜出后点「重伤」：Resolve/Morality 反向位移；panel 应有冷色 modulate
- [ ] ≥1 段 30s 录屏（含两种选择）+ ≥3 张关键截图（spare / defeat / zone 切换或 tier 切换）
- [ ] 证据放 `production/qa/evidence/s7-vs-outcome-feedback/` 同目录

## 4 · 已知限制 / Sprint 8 改进项

- 朦胧化用 `modulate` 色调 + `StyleBoxFlat` 半透明深底，**不上 ShaderMaterial 高斯模糊**（spec §2.2 Out of Scope）
- 仅在 outcome panel 内做 modulate；explore 场景的 mindset zone 染色 → 推到 sm-004 + 后续集成
- 单会话内存 only（不存档）—— 退游戏即重置
- BattleEnd 自动位移路径未启用（避免与玩家二次选择叠加）—— 严格意义的 GDD §来源 B 战斗结果位移留给 Sprint 8

## 5 · 关联 Commits

| Commit | Subtask / AC | 描述 |
|---|---|---|
| `79b9b0d` | Subtask 1 / AC1 | dev-story spec |
| `4464998` | Subtask 2 / AC5 | MindsetZoneLiteraryNames |
| `7e0200c` | Subtask 3 / AC2 | JiangnanFlowController Mindset 集成 |
| `9be8a8b` | Subtask 4 / AC3 | OutcomeGame 接入 + 文学化渲染 |
| `5002aa3` | Subtask 5 / AC4 | StyleBoxFlat + Warmth modulate |
| `86bb24a` | Subtask 6 / AC6 | Foundation 化 + 4 integration tests |

## 6 · 结论

**代码层 PASS · DoD owner 实机走查 + 录屏 pending**。
